apiVersion: v1
kind: ServiceAccount
metadata:
  name: splitx
  labels:
    app.kubernetes.io/name: splitx
    app.kubernetes.io/part-of: splitx
# The app never calls the Kubernetes API, so its pods mount no token.
# On EKS this is where the IRSA role annotation goes (phase 7).
automountServiceAccountToken: false

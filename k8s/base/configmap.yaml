apiVersion: v1
kind: ConfigMap
metadata:
  name: splitx-config
  namespace: splitx
data:
  NODE_ENV: "production"
  NEXT_TELEMETRY_DISABLED: "1"
  PORT: "3000"
  # METRICS_TOKEN is a secret — it lives in splitx-secrets, not here.
